import React, { useState, useEffect, useRef } from 'react';
import { withRouter, Link, Redirect } from 'react-router-dom';

const SiginUp = () => {
    return <>SiginUp</>;
};

export default withRouter(SiginUp);
